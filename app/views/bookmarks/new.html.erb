<%= simple_form_for [ @list, @bookmark ] do |f| %>
  <%= f.input :movie, collection: Movie::TITLE, prompt: "Please select a movie" %>
  <%= f.submit "Submit bookmark", class: "btn btn-primary" %>
  <%= f.input :comment %>
<% end %>
